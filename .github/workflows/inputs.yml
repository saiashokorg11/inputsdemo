name: inputdemo
on: 
  workflow_dispatch:
    inputs:
      environments:
        description: choose the environments tp deploy
        required: true
        default: dev

jobs:
  inputs-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the app
        run: echo "Depoly the application to ${{inputs.environments}}"
